1. when your collaborationg with a classmate you have to use git branch [name]
2. developers uses branchs to stay orgainized 
3. git is a command line tool to keep track of files and it works together with github by helping save work and their able to go back to their old changes
4. the issue with the command sequnce is that it is in the wrong sequence is he needs to swap `git commit -m"saving work` with  `git add.`.The correct sequence is 
```
git add .
git commit -m "saving work"
git push
```